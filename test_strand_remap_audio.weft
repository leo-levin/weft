// Test image-to-audio conversion using strand remapping
// This would work with an actual image loaded with load()

// Simulate a simple 2D pattern for testing
sin(me@x * 10) * cos(me@y * 8) :: pattern<r,g,b>

// Convert 2D pattern to 1D audio using scanline approach
pattern@r(me@sample % 64, floor(me@sample / 64)) :: scanline<audio>

// Display the pattern and play the audio
display(pattern@r, pattern@g, pattern@b)
play(scanline@audio)